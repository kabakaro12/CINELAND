{% extends "layout.html.twig" %}

{% block titre %}

	<title>Film</title>
	Film
{% endblock %}

{% block corps %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ film.titre }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ film.duree }}</td>
            </tr>
            <tr>
                <th>DateSortie</th>
                <td>{{ film.dateSortie.format('d-m-Y') }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ film.note }}</td>
            </tr>
            <tr>
                <th>AgeMinimal</th>
                <td>{{ film.ageMinimal }}</td>
            </tr>
			<tr>
				<th>Acteurs</th>
				<td> {% for acteur in film.acteurs %} {{ acteur }}, {% endfor %}</td>
			</tr>
        </tbody>
    </table>
	<a href="{{ path('film_edit', {'id': film.id}) }}">Editer</a>
	
    {{ include('film/_delete_form.html.twig') }}
	
	
{% endblock %}

{% block pied %}

    <a href="{{ path('film_index') }}">Retour à la liste</a>
	
{% endblock %}
